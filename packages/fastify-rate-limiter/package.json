{
  "name": "fastify-rate-limiter",
  "version": "1.0.0",
  "description": "A Fastify rate limiter plugin with support for multiple strategies (fixed window, token bucket) and Redis storage",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "fastify",
    "rate-limiter",
    "redis",
    "token-bucket",
    "fixed-window",
    "middleware"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "fastify-plugin": "^4.5.1",
    "ioredis": "^5.3.2"
  },
  "peerDependencies": {
    "fastify": "^4.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.5",
    "fastify": "^4.25.2",
    "ioredis-mock": "^8.9.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
