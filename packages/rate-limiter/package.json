{
  "name": "@anthropic/rate-limiter",
  "version": "1.0.0",
  "description": "A Fastify rate limiting middleware with pluggable strategies (fixed-window, token-bucket) and Redis storage",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {
    "fastify": ">=4.0.0",
    "ioredis": "^5.0.0"
  },
  "dependencies": {
    "fastify-plugin": "^5.0.0"
  },
  "devDependencies": {
    "fastify": "^4.28.0",
    "ioredis": "^5.4.1",
    "ioredis-mock": "^8.9.0",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  },
  "license": "MIT"
}
