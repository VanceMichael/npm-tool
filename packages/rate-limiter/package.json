{
  "name": "@your-org/fastify-rate-limiter",
  "version": "1.0.0",
  "description": "Fastify rate limiter plugin with Redis storage supporting fixed-window and token-bucket algorithms",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "fastify",
    "rate-limit",
    "redis",
    "token-bucket",
    "fixed-window",
    "middleware"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "peerDependencies": {
    "fastify": "^4.0.0"
  },
  "dependencies": {
    "fastify-plugin": "^4.5.1",
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "fastify": "^4.27.0",
    "ioredis-mock": "^8.9.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
